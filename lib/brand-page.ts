/**
 * Размер страницы товаров бренда.
 *
 * Держим в отдельном модуле, а не в шаблоне: шаблон помечен 'use client',
 * и его экспорты в серверном компоненте становятся client-reference,
 * то есть в запрос уходило бы не число, а прокси — лимит молча терялся.
 */
export const BRAND_PAGE_SIZE = 24;
